# Prompt Log — Лабораторная работа №13, Вариант 10

## Задание 1: Определение агентов

### Промпт 1
**Инструмент:** Claude Code  
**Промпт:** "Опиши роли агентов для ресторана (вариант 10): приём заказа, кухня, столы, доставка. Укажи топики NATS, входные/выходные данные, бизнес-правила."  
**Результат:** Получена таблица агентов с описанием. Решил добавить проверку двойного занятия стола как отдельное бизнес-правило.  
**Исправил вручную:** Добавил правило `table_id ∈ [1..20]` вместо просто `> 0`.  
**Время:** ~10 мин

---

## Задание 2: Go-агент (прототип)

### Промпт 2
**Инструмент:** Claude Code  
**Промпт:** "Реализуй агент приёма заказа на Go: подписка на NATS, десериализация JSON-задачи, валидация (table_id > 0, items не пустые, qty > 0, price ≥ 0), расчёт суммы, публикация результата."  
**Результат:** Создан `order.go` с чистой функцией `processOrder`. Сразу разделил на чистую функцию и NATS-обёртку для тестируемости.  
**Исправил вручную:** Вынес общую NATS-логику в `main.go`, чтобы не дублировать для каждого агента.  
**Время:** ~20 мин

### Промпт 3
**Инструмент:** Claude Code  
**Промпт:** "Добавь агентов kitchen, table, delivery по той же схеме. kitchen проверяет меню, table управляет состоянием столов через TableState (потокобезопасно), delivery назначает официанта."  
**Результат:** Созданы `kitchen.go`, `table.go`, `delivery.go`. TableState использует `sync.Mutex`.  
**Исправил вручную:** Убрал глобальный `tables map` в пользу struct, чтобы тесты не мешали друг другу.  
**Время:** ~15 мин

---

## Задание 3: Оркестратор на Python

### Промпт 4
**Инструмент:** Claude Code  
**Промпт:** "Создай AgentOrchestrator на Python (asyncio + nats-py): connect, send_task(type, payload, timeout, retries), _on_result callback, processed_count."  
**Результат:** Создан `orchestrator.py`. `send_task` использует `asyncio.Future` и `wait_for`.  
**Исправил вручную:** Заменил устаревший `asyncio.get_event_loop()` на `asyncio.get_running_loop()`.  
**Время:** ~15 мин

---

## Задание 4: NATS через Docker Compose

### Промпт 5
**Инструмент:** Claude Code  
**Промпт:** "Напиши docker-compose.yml: nats:2.10, агенты (Go), api (Python). Два кухонных агента для балансировки. Healthcheck на NATS."  
**Результат:** `docker-compose.yml` с 7 сервисами, healthcheck через wget к /healthz.  
**Исправил вручную:** ничего.  
**Время:** ~10 мин

---

## Задание 5: Логирование и метрики

### Промпт 6
**Инструмент:** Claude Code  
**Промпт:** "Добавь в агент: логирование INFO/ERROR в stdout + опциональный файл через --log флаг; потокобезопасный счётчик обработанных задач."  
**Результат:** `metrics.go` (Counter на atomic), `io.MultiWriter` в `main.go`. Флаг `--log`.  
**Исправил вручную:** ничего.  
**Время:** ~10 мин

---

## Задание 6: Retry и таймаут

### Промпт 7
**Инструмент:** Claude Code  
**Промпт:** "В send_task: retry до 3 раз при сбое агента или таймауте. Каждая попытка — новый task_id."  
**Результат:** Цикл `for attempt in range(1, retries+1)` с новым future на каждую попытку.  
**Исправил вручную:** При агентской ошибке (`success=false`) pending-запись уже удалена в `_on_result` — убедился, что никакого двойного удаления нет.  
**Время:** ~10 мин

---

## Задание 7: Несколько агентов одного типа

### Промпт 8
**Инструмент:** Claude Code  
**Промпт:** "Обеспечь балансировку нагрузки между двумя кухонными агентами через NATS."  
**Результат:** Изменил `Subscribe` на `QueueSubscribe(subject, agentType+"-workers", ...)`. Два сервиса в docker-compose.  
**Исправил вручную:** ничего.  
**Время:** ~5 мин

---

## Задание 8: REST API на FastAPI

### Промпт 9
**Инструмент:** Claude Code  
**Промпт:** "Оберни оркестратор в FastAPI: POST /orders, /kitchen, /tables, /delivery; GET /stats. Используй lifespan для connect/close."  
**Результат:** `api.py` с Pydantic-моделями и lifespan context manager.  
**Исправил вручную:** Убрал устаревший `@app.on_event` в пользу `@asynccontextmanager async def lifespan`.  
**Время:** ~15 мин

---

## Задание 9: Тестирование

### Промпт 10
**Инструмент:** Claude Code  
**Промпт:** "Напиши table-driven тесты Go для processOrder, processKitchen, processTable, processDelivery, Counter. Pytest с mock NATS для оркестратора."  
**Результат:** 5 файлов `*_test.go`, `tests/test_orchestrator.py`. Все тесты зелёные.  
**Исправил вручную:** Добавил `TestProcessTableFreeAfterOccupy` — отдельный сценарий освобождения стола. В pytest-тестах использовал `_resolve_on_publish` helper чтобы не дублировать fake_publish логику.  
**Время:** ~20 мин

---

## Задание 10: Документация архитектуры

### Промпт 11
**Инструмент:** Claude Code  
**Промпт:** "Создай docs/architecture.md с таблицей агентов и Mermaid-диаграммой взаимодействия. Опиши балансировку и обработку ошибок."  
**Результат:** `docs/architecture.md` с таблицей, Mermaid-диаграммой и описанием каждого компонента.  
**Исправил вручную:** ничего.  
**Время:** ~10 мин

---

## Итого

- **Промптов:** 11
- **Исправлял вручную:** замена get_event_loop → get_running_loop; вынос общей NATS-логики; глобальный map → TableState struct; on_event → lifespan; дополнительные тест-сценарии
- **Время:** ~2 ч
